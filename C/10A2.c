#include <stdio.h>
void main()
{
    int num, sum = 0, count = 0;
    char choice;
    do
    {
        printf("enter a number: ");
        scanf("%d", &num);
        sum += choice;
        count++;
        printf("enter y:");
        scanf("%c",choice);
    } while (choice = 'y' || choice = 'Y')
    
}